# Active Plan — Representative cognitive-decompression experiment

- Active hypothesis: decomposing dense source prose into small grounded knowledge nodes plus explicit empty gaps will break rewrite imitation and provide a better substrate for later teaching synthesis than direct rewriting.
- Current approach: `graph.json` is always written after an extraction attempt. `sourceUnitIds` and raw `sourceUnits` are the sole provenance contract; model node text is semantic extraction and is never quote-validated. Strict validation protects JSON structure, source-unit IDs, node content, and graph topology. A typed salvage pass preserves parseable node data, annotations, and bounded `metadata.salvageIssues`.
- Validation path: require valid JSON, complete source-unit coverage, valid retained edges/evidence frames, explicit empty gaps, node annotations, and `metadata.salvageIssues`. A later pass may interpret annotations but must not turn them into factual claims without evidence.
- Current checkpoint: Qbits has a complete definition-resolution overlay at `articles/qbits/definition-resolution-overlay.complete.json`: all 1,694 definition gaps have definitions; 426 are source-backed or reachable externally cited resolutions, while 1,268 remain explicitly `citation_needed` model/context candidates. The authoritative graph remains unchanged.
- Negative-memory constraints: do not abandon an extraction because a chunk has recoverable node-level errors; retain raw model artifacts, annotate rather than silently discard invalid topology, preserve source coverage, and do not auto-fill gaps.
- Assignment: primary agent owns integration and validation; no delegation requested.
